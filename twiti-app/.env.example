# Set this after publishing the Sui Move contract (see README Smart Contracts section).
# Without it, the app still works using direct SUI transfer, but for HackMoney judging
# we recommend deploying the contract and setting this so the app uses the Move module and emits on-chain Payment events.
NEXT_PUBLIC_SUI_ENS_PAY_PACKAGE_ID=0x...
